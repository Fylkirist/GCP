{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "$id": "http://sap.com/vflow/ak.emu.slt_emulator_tu_sr_act.configSchema.json",
    "type": "object",
    "properties": {
        "codelanguage": {
            "type": "string"
        },
        "script": {
            "type": "string"
        },
        "replicationmode": {
            "title": "Replication Mode",
            "description": "Same options as the SLT Operator ",
            "type": "string",
            "enum": [
                "Delta",
                "Replication",
                "Initial"
            ]
        }
    },
    "required": [
        "replicationmode"
    ]
}